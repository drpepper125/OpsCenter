fastapi==0.111.0
uvicorn[standard]==0.30.1

# Cloud SDKs (start with AWS, expand later)
boto3==1.34.103

# Optional: background task queue (for parallel ops)
celery==5.4.0
redis==5.0.4

# Security (JWT Auth)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Pydantic for request/response models (included with FastAPI, pinned here for clarity)
pydantic==2.7.1

# Logging / Utils
python-dotenv==1.0.1
